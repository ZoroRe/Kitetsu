package me.zoro.kitetsu.json;

import com.google.gson.JsonObject;
import org.json.JSONArray;
import org.json.JSONObject;
import org.json.JSONString;

/**
 * @author luguanquan
 * @date 2020-04-12 18:37
 */
public class JsonReadme {

	/**
	 * 主要是之前面试被提问过整个 json 解析，然后提到 json 下有哪些类。所以这里翻看一下这个 java 自带的 json 框架
	 * 结果现在发现这个和当时面试官说的也不一样，哈哈。。。
	 * 另外也可以对比 fastjson gson 源码了解下
	 */
	public void readJsonSourceCode() {
		JSONObject obj = new JSONObject();
		JSONArray array = new JSONArray();
	}
}
